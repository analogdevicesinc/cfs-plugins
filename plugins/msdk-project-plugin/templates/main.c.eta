<%~ include("@copyrightHeader", it) %>
<%
function getCoreSuffix() {
  if (it.soc.toLowerCase() === "max32666" && it.coreId === "CM4-1") {
    return "_core1";
  }
  return "";
}
%>

/* Include global variables and configurations for the SoC.
*/
#include "../soc_init<%= getCoreSuffix() %>.h"

int main<%= getCoreSuffix() %>(void) {
  return 0;
}
