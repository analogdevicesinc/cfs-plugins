{
  "name": "cfs-plugins-api",
  "publisher": "adi",
  "description": "Plugin API for CodeFusion Studio",
  "author": "Analog Devices",
  "version": "2.0.1",
  "scripts": {
    "build": "tsc -b ./tsconfig.json",
    "clean": "shx rm -rf dist",
    "lint": "eslint . --ext .ts",
    "test": "mocha --require ts-node/register --recursive src/**/*.test.ts"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "type": "module",
  "license": "Apache-2.0",
  "dependencies": {
    "eta": "^3.5.0",
    "glob": "^11.0.1"
  }
}
